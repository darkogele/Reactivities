
Dodavame Migracija so odredeno ime InitialCreate da se naoga vo folderot 
so -P (P za PROJECT )a da ima konekcija od proektot -S (S za start up project)

dotnet ef migrations add InitialCreate -p Persistence/ -s API/